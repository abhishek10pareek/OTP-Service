const message = (otp) => {
    return `Dear User, \n\n`
    + `${otp} is your otp for Reset Password. Please enter the OTP to proceed.\n\n`
    + `Regards\n`
    + `Abhishek Pareek` 
}

module.exports = message;